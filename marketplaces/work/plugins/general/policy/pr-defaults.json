{
  "defaultReviewers": ["${env:ADO_LEAD_ID}"],
  "mergeStrategy": "squash",
  "autoComplete": false,
  "deleteSourceBranch": true,
  "workItemLinking": "required",
  "requiredChecks": ["betterleaks", "build"]
}
