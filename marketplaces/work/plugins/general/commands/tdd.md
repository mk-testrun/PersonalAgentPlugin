# /tdd

Erzwinge den Red-Green-Refactor-Zyklus für die aktuelle Aufgabe.

Ruft `tdd-loop` auf. Schreibt erst den fehlschlagenden Test (RED), dann Minimum-Implementierung (GREEN), dann Refactor. Nutzt `testing/dotnet-test-run`.
